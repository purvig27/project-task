import { SearchFliterPipe } from './search-fliter.pipe';

describe('SearchFliterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFliterPipe();
    expect(pipe).toBeTruthy();
  });
});
